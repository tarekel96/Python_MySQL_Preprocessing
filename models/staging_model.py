# represents an StagingModel table of the DB
class StagingModel():
        def __init__(self, expedition, astronaut, age, gender, 
        nationality, duration, agency, agency_origin) -> None:
                self.id = id
                self.expedition = expedition
                self.astronaut = astronaut
                self.age = age
                self.gender = gender
                self.nationality = nationality
                self.duration = duration
                self.agency = agency
                self.agency_origin = agency_origin